# My Basic Vim Config File

I usually use neovim, however when that is unavailable I use vim!

## Prerequisites
- fzf

## Install
```sh
git clone https://github.com/Phandal/PHVIM
cd PHVIM
ln -s ./.vimrc ~/.vimrc
ln -s ./.vim ~/.vim
```
